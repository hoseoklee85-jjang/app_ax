export default function Dashboard() {
  return (
    <section className="admin-panel" style={{ gridColumn: '1 / -1' }}>
      <h2>대시보드</h2>
      <p style={{ color: 'var(--text-muted)' }}>통계 및 대시보드 화면이 준비 중입니다...</p>
    </section>
  );
}
